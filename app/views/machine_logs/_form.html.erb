<%= form_for(@machine_log) do |f| %>
  <% if @machine_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_log.errors.count, "error") %> prohibited this machine_log from being saved:</h2>

      <ul>
      <% @machine_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br />
    <%= f.text_field :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
