<%= form_for(@milestone,:multipart=> true) do |f| %>
  <% if @milestone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@milestone.errors.count, "error") %> prohibited this milestone from being saved:</h2>

      <ul>
      <% @milestone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %><br>

    <div class="field">
      <p>
      <%= f.label :title %>
      <%= f.text_field :title%>
      </p>
    </div>
    <div class="field">Languages</div>
    <%@languages.each do |lang|%>
        <div class="field">
          <%= label_tag lang%>
          <%= text_field_tag lang%>
        </div>
    <%end%>

    <div class="field">
      <p>
        <%=f.label :image%>
        <%= f.file_field :image%>
      </p>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
